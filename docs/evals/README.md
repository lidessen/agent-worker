# Dev Evals

This directory keeps the durable parts of local development evaluations:

- reusable tooling
- reusable scorecard templates
- condensed findings and trends
- a small number of representative case studies

Raw runtime artifacts such as JSONL logs, blind mappings, worktree paths, and patch diffs stay under local `.agent-workspace/` directories and are intentionally not versioned here.
